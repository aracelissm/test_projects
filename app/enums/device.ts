export enum EDeviceType {
    Dispatcher = 0,
    Reserved = 18
}

export enum EDeviceTitle {
    Dispatcher = 'Dispatcher',
    Reserved = 'Reserved',
    ROOF = 'ROOF'
}

export enum EEventType {
    Break = 'Break',
    Lunch = 'Lunch',
    Maintenance = 'Maintenance'
}
